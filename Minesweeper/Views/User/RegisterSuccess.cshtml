@{
    ViewData["Title"] = "Registration Success";
}

<div class="mt-5 p-4 bg-white mx-auto rounded shadow">
    <h2 class="text-success text-center fw-semibold">Registration Successful!</h2>

    <p class="mt-3 text-center fs-5">
        Your account has been created successfully. You can now log in to start using the application.
    </p>

    <div class="mt-4 text-center">
        <a href="@Url.Action("Login", "User")" class="text-primary text-decoration-underline">Go to Login Page</a>
    </div>
</div>
